# Fake-Stock

# 📈 Fake Stock Tracker (JavaScript)

A simple Fake Stock Tracker built using **Vanilla JavaScript** that simulates live stock price updates using random values and time-based functions.

## 🚀 Features
- Generates random stock prices dynamically
- Updates stock value at fixed intervals
- Displays simulated time-based price changes
- Uses JavaScript core concepts only (no libraries)

## 🛠️ Technologies Used
- HTML
- CSS
- JavaScript

## 🧠 Concepts Covered
- `Math.random()`
- `setInterval()`
- DOM Manipulation
- Time-based updates
- Basic simulation logic

## 📊 How It Works
- A random stock price is generated using `Math.random()`
- `setInterval()` updates the stock value after a fixed time interval
- DOM is updated dynamically to reflect price changes
- Mimics real-time stock movement behavior

